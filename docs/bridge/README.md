# Bridge

Bridge 文档记录外部桥接相关能力的边界。当前阶段 Bridge 不属于默认终端主链路。

## 当前状态

- 保留目录和历史代码。
- 不随 `elebot` 默认启动。
- 不作为核心 CLI、Agent、Provider 验收目标。

## 边界

Bridge 后续可以用于外部系统接入，但在核心对话链路稳定前，不应把它接回默认入口。

## 文档规则

如果 Bridge 后续解冻，需要补充：

- 入口方式。
- 消息格式。
- 鉴权边界。
- 与 Agent 主链路的调用关系。
- 对应测试文档。
