# Command

Command 模块负责解析交互模式里的内部命令，例如退出、切换模型、查看会话等。

## 当前职责

- 识别 slash 命令。
- 将命令路由到对应处理函数。
- 返回 CLI 可以展示的结构化结果。

## 代码边界

- 不处理 prompt_toolkit 输入细节。
- 不直接调用底层 Provider。
- 不保存会话历史，必要时通过 Session 模块完成。

## 当前原则

命令面只暴露当前已接通的能力，冻结模块的命令不进入默认帮助和交互列表。
