# Bus

Bus 模块提供轻量事件通信能力，用于在 Agent、CLI 和内部服务之间传递状态。

## 当前职责

- 定义事件结构。
- 分发流式正文、thinking、工具进度和错误事件。
- 降低模块之间的直接耦合。

## 代码边界

- Bus 不保存业务状态。
- Bus 不执行工具。
- Bus 不渲染终端。

## 维护要求

事件字段必须保持稳定；新增事件时需要同步更新 Agent、CLI 和测试文档。
