# Session

Session 模块负责本地会话、历史记录和恢复能力。

## 当前职责

- 创建和读取会话。
- 追加用户消息、助手消息和工具结果。
- 保存多轮对话历史。
- 支持交互模式恢复最近会话。

## 默认路径

```text
~/.elebot/sessions
```

## 代码边界

- Session 不负责模型调用。
- Session 不负责压缩策略之外的推理决策。
- Session 不渲染终端输出。

## 验收重点

- 多轮对话后历史可恢复。
- 会话文件损坏时应返回稳定错误，不让 CLI 崩溃。
