# Facade

Facade 模块是应用装配边界，用于把配置、会话、Provider、Agent 和工具注册组合成可运行对象。

## 当前职责

- 加载配置。
- 初始化会话管理器。
- 初始化 Provider。
- 注册核心工具。
- 构建 Agent 运行入口。

## 代码边界

- Facade 只做装配，不写业务细节。
- Facade 不直接渲染终端。
- Facade 不实现具体工具逻辑。

## 维护要求

如果一个依赖需要进入主链路，必须先判断它是否属于当前阶段的 Active 模块。
