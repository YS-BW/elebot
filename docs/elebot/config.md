# Config

Config 模块负责加载 EleBot 的本地配置，并提供运行目录、模型配置和默认值。

## 当前职责

- 读取 `~/.elebot/config.json`。
- 初始化缺失的默认配置。
- 提供默认模型、Provider 参数和本地运行目录。
- 为 CLI、Agent、Session、Provider 提供统一配置对象。

## 代码边界

- 不读取旧项目的 TOML 配置。
- 不做迁移脚本。
- 不在模块内部发起模型调用。
- 不在模块内部处理终端交互。

## 维护要求

配置字段变化时，必须同步更新默认配置模板和相关测试。
